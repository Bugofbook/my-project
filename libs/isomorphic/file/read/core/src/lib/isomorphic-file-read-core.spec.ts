import { isomorphicFileReadCore } from './isomorphic-file-read-core';

describe('isomorphicFileReadCore', () => {
  it('should work', () => {
    expect(isomorphicFileReadCore()).toEqual('isomorphic-file-read-core');
  });
});
