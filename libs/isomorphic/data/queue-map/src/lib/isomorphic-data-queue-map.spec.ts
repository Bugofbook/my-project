import { isomorphicDataQueueMap } from './isomorphic-data-queue-map';

describe('isomorphicDataQueueMap', () => {
  it('should work', () => {
    expect(isomorphicDataQueueMap()).toEqual('isomorphic-data-queue-map');
  });
});
